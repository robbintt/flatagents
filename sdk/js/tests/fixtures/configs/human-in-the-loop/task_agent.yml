spec: flatagent
spec_version: "0.6.0"
data:
  name: task_agent
  model:
    name: gpt-4o-mini
    provider: openai
  system: "You are a task planning assistant. Generate a task plan based on the request."
  user: "Request: {{ input.request }}\n\nGenerate a task plan:"
  output:
    plan:
      type: json
      description: "Task plan with steps"