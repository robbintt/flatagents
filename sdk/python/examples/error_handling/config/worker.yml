spec: flatagent
spec_version: "0.7.5"

data:
  name: worker
  model: default
  system: You analyze requests and provide responses.
  user: |
    Analyze: {{ input.task }}
  output:
    result:
      type: str
      description: Analysis result
