spec: flatagent
spec_version: "0.6.0"

data:
  name: worker
  model:
    provider: cerebras
    name: zai-glm-4.6
    temperature: 1.0
  system: You analyze requests and provide responses.
  user: |
    Analyze: {{ input.task }}
  output:
    result:
      type: str
      description: Analysis result
