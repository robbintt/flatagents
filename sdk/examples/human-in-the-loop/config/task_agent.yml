spec: flatagent
spec_version: "1.0.0"
data:
  name: task_agent
  model: default
  system: "You are a task planning assistant. Generate a task plan based on the request."
  user: |
    Request: {{ input.request }}

    Generate a task plan with steps to accomplish this request.

metadata:
  author: flatagents
  description: Task planning assistant agent