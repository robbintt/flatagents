spec: flatagent
spec_version: "0.9.0"

data:
  name: worker
  model: default
  system: You analyze requests and provide responses.
  user: |
    Analyze: {{ input.task }}

    Provide your analysis.

metadata:
  author: flatagents
  description: Worker agent that analyzes tasks
