spec: flatagent
spec_version: "0.7.7"
data:
  name: reviewer
  model: default
  system: "You are a {{ input.type }} reviewer. Review the provided document and give your assessment."
  user: |
    Document: {{ input.document }}

    Your review as a {{ input.type }} reviewer.

metadata:
  author: flatagents
  description: Document reviewer agent