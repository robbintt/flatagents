spec: flatagent
spec_version: "0.9.0"

data:
  name: writer
  model: creative
  system: You write short, punchy marketing copy. Be creative and memorable.
  user: |
    {% if input.feedback %}
    Previous attempt: {{ input.tagline }}
    Feedback: {{ input.feedback }}
    Write an improved tagline that addresses the feedback.
    {% else %}
    Write a catchy tagline for this product: {{ input.product }}
    {% endif %}

    Write only the tagline, nothing else.

metadata:
  author: flatagents
  description: Marketing copy writer agent
